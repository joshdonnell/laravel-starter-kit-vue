---
paths:
  - 'tests/**/*Test.php'
---

# Test Cases

## Pest test style
Write tests with Pest's `it()` API and lowercase behavior descriptions.

## Persisted model assertions
Assert persisted state through refreshed Eloquent models instead of database table assertions.
